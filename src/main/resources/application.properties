spring.application.name=pets

# --- PostgreSQL (Neon) ---
spring.datasource.url=jdbc:postgresql://ep-shy-sky-agfv3s9a-pooler.c-2.eu-central-1.aws.neon.tech:5432/pets?sslmode=require
spring.datasource.username=app
spring.datasource.password=Kino32016996@
spring.datasource.driver-class-name=org.postgresql.Driver

# (opcional, pero recomendable dejarlo explícito)
spring.jpa.properties.hibernate.default_schema=app
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- JPA ---
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Swagger ---
server.error.include-message=always
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# --- Security / JWT ---
security.jwt.secret=${JWT_SECRET:CHANGE_ME}
security.jwt.expiration-minutes=120
app.frontend.base-url=${FRONTEND_BASE_URL:http://localhost:4200}
app.mail.from=${MAIL_FROM:appets85@gmail.com}
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:appets85@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
